# 🔒 Безопасность

## ⚠️ ВАЖНО: Защита чувствительных данных

### 🚨 Перед развертыванием

**ОБЯЗАТЕЛЬНО** выполните следующие шаги:

1. **Измените пароль админ-панели**
   ```bash
   # Создайте файл .env на основе примера
   cp .env.example .env
   
   # Отредактируйте .env и установите свой пароль
   nano .env
   ```

2. **Укажите свой домен**
   ```bash
   # В файле .env укажите ваш реальный домен
   DOMAIN=your-domain.com
   ```

3. **НЕ коммитьте .env в Git**
   ```bash
   # Файл .env уже в .gitignore, но проверьте:
   git status
   # .env НЕ должен быть в списке изменений
   ```

## 🔐 Переменные окружения

Проект использует переменные окружения для хранения чувствительных данных:

| Переменная | Описание | Обязательна |
|------------|----------|-------------|
| `DOMAIN` | Ваш домен | ✅ Да |
| `ADMIN_PASSWORD` | Пароль админки | ✅ Да |
| `LETSENCRYPT_EMAIL` | Email для SSL | ✅ Да |
| `TZ` | Часовой пояс | ⚪ Нет |

## 🛡️ Рекомендации по безопасности

### 1. Сильный пароль
```bash
# Используйте генератор паролей
openssl rand -base64 32
```

### 2. Регулярно обновляйте
```bash
# Обновляйте Docker образы
docker-compose pull
docker-compose up -d
```

### 3. Мониторинг логов
```bash
# Проверяйте логи на подозрительную активность
docker-compose logs -f
```

### 4. Firewall
```bash
# Используйте UFW или iptables
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 8443/tcp
ufw allow 8444/tcp
ufw enable
```

### 5. Fail2ban
```bash
# Защита от брутфорса
apt install fail2ban
systemctl enable fail2ban
```

## 🚫 Что НЕ нужно хранить в Git

- ❌ Пароли
- ❌ API ключи
- ❌ SSL сертификаты
- ❌ Приватные ключи
- ❌ Файл `.env`
- ❌ IP адреса серверов
- ❌ Реальные домены (если они личные)

## ✅ Что можно хранить в Git

- ✅ Код приложения
- ✅ Конфигурационные шаблоны
- ✅ Документация
- ✅ Dockerfile и docker-compose.yml
- ✅ `.env.example` (с примерами)

## 🔄 Что делать если пароль попал в Git

1. **Немедленно смените пароль** на сервере
2. Удалите пароль из истории Git:
   ```bash
   # Используйте git-filter-repo или BFG Repo-Cleaner
   git filter-repo --replace-text passwords.txt
   ```
3. Force push (ОСТОРОЖНО!):
   ```bash
   git push --force origin main
   ```

## 📞 Сообщить о проблеме безопасности

Если вы нашли уязвимость, пожалуйста, НЕ создавайте публичный issue.

Свяжитесь с автором напрямую через GitHub.

---

**Помните**: Безопасность - это процесс, а не результат. Регулярно проверяйте и обновляйте систему!

