# 🐳 VPN Server - Docker Installation Guide

Полная инструкция по развертыванию VPN сервера с использованием Docker на любом сервере.

## 📋 Содержание

1. [Требования](#требования)
2. [Быстрая установка](#быстрая-установка)
3. [Ручная установка](#ручная-установка)
4. [Настройка](#настройка)
5. [Управление](#управление)
6. [Решение проблем](#решение-проблем)

---

## 🎯 Требования

### Сервер
- **ОС**: Ubuntu 20.04+, Debian 11+, CentOS 8+
- **RAM**: минимум 1GB (рекомендуется 2GB)
- **CPU**: 1 vCore (рекомендуется 2)
- **Диск**: минимум 20GB
- **Сеть**: публичный IP-адрес

### Домен
- Зарегистрированный домен
- DNS A-запись указывает на IP сервера
- Порты 80, 443, 8443, 8444 открыты

### Локально
- Git
- SSH клиент

---

## 🚀 Быстрая установка

### Шаг 1: Подключение к серверу

```bash
ssh root@ВАШ_IP
```

### Шаг 2: Клонирование репозитория

```bash
cd /opt
git clone https://github.com/YOUR_REPO/vpn-docker.git
cd vpn-docker
```

### Шаг 3: Запуск установки

```bash
chmod +x scripts/install.sh
./scripts/install.sh
```

Скрипт запросит:
- **Домен** (например: vpn.example.com)
- **Email** для Let's Encrypt
- **Пароль** для админ-панели

### Шаг 4: Готово!

После установки откройте:
- **Админ-панель**: https://ваш-домен.com/admin
- **Главная страница**: https://ваш-домен.com

---

## 🔧 Ручная установка

Если автоматический скрипт не подходит, выполните установку вручную:

### 1. Установка Docker

```bash
# Ubuntu/Debian
curl -fsSL https://get.docker.com | sh
systemctl start docker
systemctl enable docker

# Проверка
docker --version
docker-compose --version
```

### 2. Подготовка проекта

```bash
# Клонирование
cd /opt
git clone https://github.com/YOUR_REPO/vpn-docker.git
cd vpn-docker

# Создание директорий
mkdir -p data/{xray,nginx,certs,logs/{xray,nginx},users,certbot-webroot}
```

### 3. Настройка конфигурации

```bash
# Создание .env файла
cat > .env << EOF
DOMAIN=vpn.example.com
EMAIL=admin@example.com
ADMIN_PASSWORD=your_secure_password
TZ=Europe/Moscow
EOF

# Обновление домена в Nginx
sed -i "s/DOMAIN/vpn.example.com/g" nginx/default.conf

# Обновление пароля админки
sed -i "s/vpnadmin2024/your_secure_password/g" web/admin/index.php
sed -i "s/vpnadmin2024/your_secure_password/g" web/admin/api.php
```

### 4. Запуск контейнеров

```bash
# Сборка и запуск
docker-compose up -d

# Проверка статуса
docker-compose ps
```

### 5. Получение SSL сертификата

```bash
# Получение сертификата
docker-compose run --rm certbot certonly \
    --webroot \
    --webroot-path=/var/www/certbot \
    --email admin@example.com \
    --agree-tos \
    --no-eff-email \
    -d vpn.example.com

# Копирование для Xray
docker cp vpn-certbot:/etc/letsencrypt/live/vpn.example.com/fullchain.pem data/xray/certs/
docker cp vpn-certbot:/etc/letsencrypt/live/vpn.example.com/privkey.pem data/xray/certs/
chmod 644 data/xray/certs/*.pem

# Перезапуск
docker-compose restart
```

---

## ⚙️ Настройка

### Изменение пароля админки

```bash
nano web/admin/index.php
# Найдите: $password = 'vpnadmin2024';
# Измените на свой пароль

docker-compose restart php
```

### Добавление первого пользователя

1. Откройте админ-панель: https://ваш-домен.com/admin
2. Войдите с паролем
3. Нажмите "➕ Добавить пользователя"
4. Введите имя и email
5. Скопируйте VLESS ссылку или QR-код

### Настройка файрвола

```bash
# UFW (Ubuntu/Debian)
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 8443/tcp
ufw allow 8444/tcp
ufw enable

# Firewalld (CentOS/RHEL)
firewall-cmd --permanent --add-port=22/tcp
firewall-cmd --permanent --add-port=80/tcp
firewall-cmd --permanent --add-port=443/tcp
firewall-cmd --permanent --add-port=8443/tcp
firewall-cmd --permanent --add-port=8444/tcp
firewall-cmd --reload
```

---

## 🎮 Управление

### Основные команды

```bash
# Просмотр логов
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f xray
docker-compose logs -f nginx

# Остановка
docker-compose stop

# Запуск
docker-compose start

# Перезапуск
docker-compose restart

# Перезапуск конкретного сервиса
docker-compose restart xray

# Статус
docker-compose ps

# Удаление (с сохранением данных)
docker-compose down

# Полное удаление (включая данные)
docker-compose down -v
```

### Обновление

```bash
# Остановка
docker-compose down

# Обновление кода
git pull

# Пересборка и запуск
docker-compose up -d --build
```

### Резервное копирование

```bash
# Создание бэкапа
tar -czf vpn-backup-$(date +%Y%m%d).tar.gz data/

# Восстановление
tar -xzf vpn-backup-20251124.tar.gz
docker-compose restart
```

---

## 📊 Мониторинг

### Проверка статуса контейнеров

```bash
docker-compose ps
```

Все контейнеры должны быть в состоянии "Up":
- vpn-xray
- vpn-nginx
- vpn-php
- vpn-certbot

### Проверка логов

```bash
# Xray
docker-compose logs xray | tail -50

# Nginx
docker-compose logs nginx | tail -50

# PHP
docker-compose logs php | tail -50
```

### Проверка сертификатов

```bash
# Проверка срока действия
docker exec vpn-certbot certbot certificates

# Ручное обновление
docker-compose run --rm certbot renew
docker-compose restart
```

---

## 🆘 Решение проблем

### Контейнер не запускается

```bash
# Проверка логов
docker-compose logs [имя_сервиса]

# Проверка конфигурации
docker-compose config

# Пересборка
docker-compose up -d --build --force-recreate
```

### Не работает SSL

```bash
# Проверка сертификата
ls -lh data/certs/live/ваш-домен.com/

# Проверка прав
chmod 644 data/xray/certs/*.pem

# Перезапуск
docker-compose restart
```

### Не добавляются пользователи

```bash
# Проверка прав на файл
ls -lh data/users/users.txt

# Исправление прав
chmod 666 data/users/users.txt

# Проверка логов PHP
docker-compose logs php
```

### Порты заняты

```bash
# Проверка занятых портов
netstat -tulpn | grep -E '80|443|8443|8444'

# Остановка конфликтующих сервисов
systemctl stop nginx
systemctl stop apache2

# Или измените порты в docker-compose.yml
```

### Не работает админка

```bash
# Проверка PHP
docker-compose logs php

# Проверка Nginx
docker-compose logs nginx

# Проверка прав
docker exec vpn-php ls -lh /var/www/html/admin/

# Перезапуск
docker-compose restart php nginx
```

---

## 📁 Структура проекта

```
vpn-docker/
├── docker-compose.yml          # Главный файл Docker Compose
├── .env                        # Переменные окружения (создается при установке)
├── DOCKER-INSTALL.md          # Эта инструкция
│
├── xray/                      # Xray контейнер
│   ├── Dockerfile
│   └── config.json
│
├── nginx/                     # Nginx контейнер
│   ├── Dockerfile
│   ├── nginx.conf
│   └── default.conf
│
├── web/                       # PHP + веб-файлы
│   ├── Dockerfile
│   ├── php.ini
│   ├── index.html             # Главная страница (fake site)
│   └── admin/                 # Админ-панель
│       ├── index.php
│       ├── api.php
│       └── qr.php
│
├── scripts/                   # Скрипты установки
│   └── install.sh
│
└── data/                      # Данные (создается при установке)
    ├── xray/                  # Конфигурация Xray
    ├── nginx/                 # Конфигурация Nginx
    ├── certs/                 # SSL сертификаты
    ├── users/                 # База пользователей
    └── logs/                  # Логи
        ├── xray/
        └── nginx/
```

---

## 🔐 Безопасность

### Рекомендации

1. **Смените пароль админки** сразу после установки
2. **Используйте сложный пароль** (минимум 16 символов)
3. **Регулярно обновляйте** систему и Docker образы
4. **Делайте резервные копии** конфигурации
5. **Мониторьте логи** на подозрительную активность
6. **Ограничьте доступ** к SSH (только ключи)
7. **Настройте Fail2ban** для защиты SSH

### Обновление паролей

```bash
# Админ-панель
nano web/admin/index.php
nano web/admin/api.php
docker-compose restart php

# Root на сервере
passwd root
```

---

## 📱 Клиенты для пользователей

### iOS
- **Shadowrocket** ($2.99) - лучший вариант
- **V2Box** (бесплатный)

### Android
- **v2rayNG** (бесплатный) - рекомендуется
- **V2Box** (бесплатный)

### Windows
- **v2rayN** (бесплатный) - рекомендуется
- **Nekoray** (бесплатный)

### macOS
- **V2Box** (бесплатный)
- **Qv2ray** (бесплатный)

---

## 📞 Поддержка

### Полезные ссылки

- **Xray документация**: https://xtls.github.io/
- **Docker документация**: https://docs.docker.com/
- **Let's Encrypt**: https://letsencrypt.org/

### Логи для отладки

При возникновении проблем соберите логи:

```bash
# Все логи
docker-compose logs > logs-$(date +%Y%m%d).txt

# Конфигурация
docker-compose config > config-$(date +%Y%m%d).txt

# Статус
docker-compose ps > status-$(date +%Y%m%d).txt
```

---

## 🎉 Готово!

После успешной установки у вас будет:

- ✅ Полностью функциональный VPN сервер
- ✅ Веб-панель управления пользователями
- ✅ Автоматическое обновление SSL сертификатов
- ✅ Две конфигурации: Vision (скорость) и WebSocket (обход)
- ✅ QR-коды для быстрого подключения
- ✅ Fake website для маскировки

**Откройте админ-панель и добавьте первого пользователя!**

https://ваш-домен.com/admin

---

**Дата создания**: 2025-11-24  
**Версия**: 1.0 Docker  
**Автор**: AI Assistant

